kind: pipeline
type: kubernetes
name: default

steps:
- name: test
  image: cimg/go:1.19.0
  user: 0
  environment:
    VERSION: 0.0.1_alpha
  commands:
  - make docker-buildx
  # - make test
  volumes:
  - name: dockersock
    path: /var/run
volumes:
- name: dockersock
  host:
    path: /var/run/
